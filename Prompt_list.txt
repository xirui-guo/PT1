################
# 1: 铺了一个前端
################
我想做一个网页未来可能想做app，它的目标是可以传入音频，视频和文档后，会在页面上生成多个屏幕后，用户可以自己通过打字/选择文本关键词来生成自己的笔记，最后在完成后，可以有LLM帮助summary的按钮来生成最终完整的笔记：

1. 多个框是根据传入的文件格式来定义的：
a.音频文件：生成三个小框；
第一个框显示播放/暂停，往前15秒/往后15秒；放在左上
第二个框显示LLM模型和模型下根据音频生成的文本，当用户暂停时，可以通过鼠标滑动选择文本粘贴；放在左下
第三个框显示用户可以敲打文字的文档界面，可供用户把选择文本复制过来，并支持打字功能；放在右边

b.视频文件：生成三个小框；
第一个框显示视频界面，视频界面支持截图功能并可以放到用户笔记中；放在左上
第二个框显示LLM模型和模型下根据视频生成的文本，当用户暂停时，可以通过鼠标滑动选择文本粘贴；放在左下
第三个框显示用户可以敲打文字的文档界面；可供用户把视频截图和选择文本复制过来，并支持打字功能；放在右边

c.文档文件：生成两个小框；
第一个框显示文档文本，可以通过鼠标滑动选择文本粘贴，以及图片和表格复制；放在左边
第二个框显示用户可以敲打文字的文档界面，可供用户把选择文本/图片/表格复制过来，并支持打字功能；放在右边

################
# 2：搭一个FASTapi后端
################
我希望做到前后端联动，按照你说的方法：

作为 Python 开发者，你最熟悉的路径应该是：
后端 (Python/FastAPI)：
编写一个 Python 后端，使用 openai 库或 langchain 库。
音视频转文字：调用 OpenAI 的 whisper-1 模型。
笔记总结：调用 gpt-4o 或 deepseek-chat。

前端 (React) 联动：
在 handleSummarize 函数中，将 notes 作为参数，通过 fetch 发送到你的 Python 接口，然后接收返回的真实总结。

可以帮我分别把这两个文件都写出来，也帮我把联动的底子打好吗吗

################
# 3: 铺了一个前端
################
我现在想加一个“首页”，这个页面会呈现一个日历，音频，视频和文档三个文件的个数，查看网站的热度网格（类似于github），上传文件的热度网格。在整个页面的右上脚可以显示上传文件，然后转到我们之前写的那个前端页面

整个布局是显示“Hello Kristina，”一个可爱的雪纳瑞图片
下面一排显示查看网站的热度网格（用紫色的深浅表示高低）和上传文件的热度网格（用蓝色的深浅表示高低）
下面一排显示日历：日历是按月来表示，但可以让用户选择年份，月份和current。用格子显示每天，每个格子里面会看到，今天可以复习哪些文档的内容（艾宾浩斯遗忘曲线）
下面一排是音频，视频和文档三个文件的个数，用一个柱状图来表示。

################
# 4: 改细节
################
像艾宾浩斯复习，今天有 3 份笔记需要根据遗忘曲线复习，头像的图片和hi kristina，这里其实是要根据用户使用网站的功能后自动生成的：
比如当用户登陆后，就会知道她的名字和她上传的头像（可以默认是https://tse2.mm.bing.net/th/id/OIP.CfglGno9D-h16Lxp0Es47gHaHa?rs=1&pid=ImgDetMain&o=7&rm=3）。
关于遗忘曲线的设置和今天要复习的笔记是不是要新写一个后端fastapi来整理？
